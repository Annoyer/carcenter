<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carcenter.dao.AdminDao">

    <select id="getAdminByNameAndPassword" resultType="com.carcenter.model.Admin" parameterType="String">
        SELECT * FROM customer WHERE #{name}=adminName and #{password}=password
    </select>

</mapper>
